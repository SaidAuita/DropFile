#!/bin/bash
# ==============================================================================
# DropFile — Запуск в портативном режиме без установки
# ==============================================================================

cd "$(dirname "$0")"

if [ -d ".venv" ]; then
    source .venv/bin/activate
    python3 DropFile.pyw "$@"
else
    python3 DropFile.pyw "$@"
fi
